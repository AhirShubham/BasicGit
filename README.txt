Steps to connect a local git repo to a remote repo:

1. git init
2. git add. / git commit -a -m "Commit message"
3. git commit -m "Commit message" //This will create a new branch "main"
4. git add origin "Remote repo url"
5. git branch -M